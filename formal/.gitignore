# SymbiYosys artifacts: one directory per task (<dut>_<task>/), one status
# database per job (<dut>/), and the per-DUT stamp files the Makefile uses to
# track which jobs are up to date. Everything tracked in this directory is a
# file directly in it, so ignoring all subdirectories is safe.
*/
*.stamp
